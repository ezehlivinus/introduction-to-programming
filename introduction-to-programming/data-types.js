// boolean
const isPublished = true;
console.log(('ezeh' === 'livinus')); // true

// string
let country = 'Nigeria';
let state = "Enugu"
let city = 'Amechi';
let fullName = 'Ezeh Livinus';
console.log(typeof(fullName))
let someOtherNames = 'my other names'
fullName = someOtherNames
console.log(fullName) // my other names
// Take away: three ways you can concatenate strings in JavaScript
// Takeaway: examples of arithmetic operators in JavaScript

// number
let a = 100;
let b = 150;
let c = a + b;
console.log(a +' + ' + b + ' is = ', c); // a + b =  250

// Take away: example of object in JavaScript
// Take away: Accessing and updating/modify properties of an object, 
// Definition: an object is a key/value pair data types
const cat = {
  age: 223,
  color: 'white',
  numberOfLegs: 23,
  schools: ['UNN', 'CCU'],
} // curly braces
console.log(cat)

// take example of array in JavaScript
let scores = [10, 20, 30, 40, 50];
let names = ['Ezeh', 'Livinus', 'Bobby', state];
console.log(names)
// Take away: accessing and updating/modify values/elements of an array,

// null
let thisIsNull = null;
console.log(thisIsNull); // null
// 0, '', undefined, false, null, 